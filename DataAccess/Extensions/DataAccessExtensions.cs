using System;

namespace DataAccess
{
    static class DataAccessExtensions
    {
        // //büyük küçük harf kontrolsüz içerme
        // public static bool ContainsCI(this string source, string toCheck)
        // {
        //     return source?.IndexOf(toCheck, StringComparison.CurrentCultureIgnoreCase) >= 0;
        // }
        // //büyük küçük harf kontrolsüz eşitlik
        // public static bool EqualsCI(this string source, string toCheck) => source.ToLower().Equals(toCheck);
    }
}